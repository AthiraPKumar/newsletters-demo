# Workflows

Markdown SOPs that define objectives, required inputs, tool sequences, and expected outputs.

| Workflow | Purpose |
|----------|---------|
| *(add entries as workflows are created)* | |
